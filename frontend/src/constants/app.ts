/** Product name — keep in sync with backend `src/constants/app.js`. */
export const APP_NAME = 'ACC Labour Law Compliance';

/** Short label for the sidebar brand (full name is too wide). */
export const APP_SHORT_NAME = 'ACC Labour Law';
